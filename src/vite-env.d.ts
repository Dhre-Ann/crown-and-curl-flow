/// <reference types="vite/client" />

interface Window {
  /** Optional runtime API origin for GitHub Pages when not baked into the build (set in index.html). */
  __CROWN_STUDIO_API_BASE__?: string;
}

# Shop context for local dev (subdomain simulation). Leave empty to use customer global context (dashboard, no x-shop-slug).
VITE_SHOP_SLUG=kairstyles

# Production / real multi-tenant host: set so only {slug}.yourdomain.com is treated as a shop (never user.github.io).
# VITE_SHOP_ROOT_DOMAIN=crownstudio.com
